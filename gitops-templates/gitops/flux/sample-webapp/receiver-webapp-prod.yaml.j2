{% if gitless %}
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: webapp-prod-sync
  namespace: {{ gitops_namespace }}
spec:
  type: generic
  events:
    - package
  secretRef:
    name: webhook-token
  resources:
    - apiVersion: source.toolkit.fluxcd.io/v1
      kind: OCIRepository
      name: webapp-prod
      namespace: {{ gitops_namespace }}
{% endif %}
